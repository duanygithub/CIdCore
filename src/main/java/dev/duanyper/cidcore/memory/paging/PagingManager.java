package dev.duanyper.cidcore.memory.paging;

public class PagingManager {
    public static int allocateMemory(int preferredAddress, int size) {
        long tid = Thread.currentThread().getId();
        return 0;
    }
}
